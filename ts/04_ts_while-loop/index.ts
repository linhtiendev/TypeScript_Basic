// while loop
// k biết trước số lần lập
// đk vẫn đúng loop vẫn chạy
// muốn thoát ra khỏi loop phải dùng break

// while <=> for
// dùng while khi không quan tâm đến việc phải lập bao nhiêu lần
// chỉ cần quan tâm khi nào thoát ra để truyền cho nó điều kiện

let counter = 0;
while (counter < 5 ) {
    console.log(counter);
    counter++;
}