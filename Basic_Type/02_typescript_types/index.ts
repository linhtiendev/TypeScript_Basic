const message: string = "typescript type";
console.log(message);

const username = "liti";
console.log('check name', username.length);
console.log('check name', username.toUpperCase());

// vì data type của biến đã được khai báo từ đầu
// dùng ts phải quan tâm đến datatype là gì
// nhờ có data type ts sẽ biết các properties và method có sẵn ứng với data type đấy

